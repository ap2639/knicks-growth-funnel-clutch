# Dashboard Build Notes

The live dashboard for this project is:

- `docs/index.html`

GitHub Pages deploys the `docs/` folder directly, so `docs/index.html` is the source of truth for the public site.

The older `dashboard/index.html` artifact is no longer the deployed version and should be treated as legacy output only.
